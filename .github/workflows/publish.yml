name: Publish distributions

on:
  # Bootstrap TestPyPI before this workflow reaches the default branch. This
  # branch-only trigger is removed after the release candidate is verified.
  push:
    branches:
      - codex/innertrace-star-readiness
  workflow_dispatch:
    inputs:
      target:
        description: Package index
        required: true
        type: choice
        options:
          - testpypi
          - pypi
      source_run_id:
        description: TestPyPI workflow run containing the verified release-dist artifact (required for PyPI)
        required: false
        type: string

permissions:
  contents: read

jobs:
  build:
    if: github.event_name == 'push' || inputs.target == 'testpypi'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade build twine
      - run: python -m build
      - run: python -m twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: release-dist
          path: dist/*
          if-no-files-found: error
          retention-days: 30

  publish:
    if: >-
      always() &&
      (((github.event_name == 'push' || inputs.target == 'testpypi') &&
        needs.build.result == 'success') ||
       (github.event_name == 'workflow_dispatch' && inputs.target == 'pypi' &&
        inputs.source_run_id != ''))
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event_name == 'push' && 'testpypi' || inputs.target }}
      url: ${{ (github.event_name == 'push' || inputs.target == 'testpypi') && 'https://test.pypi.org/p/innertrace' || 'https://pypi.org/p/innertrace' }}
    permissions:
      actions: read
      id-token: write
    steps:
      - name: Download distributions built for TestPyPI
        uses: actions/download-artifact@v4
        with:
          name: release-dist
          path: dist
          run-id: ${{ github.event_name == 'workflow_dispatch' && inputs.target == 'pypi' && inputs.source_run_id || github.run_id }}
          github-token: ${{ github.token }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate distributions and release tag
        env:
          PUBLISH_TARGET: ${{ github.event_name == 'push' && 'testpypi' || inputs.target }}
          RELEASE_REF: ${{ github.ref_name }}
        run: |
          python -m pip install --upgrade twine
          python -m twine check dist/*
          python - <<'PY'
          import email.parser
          import glob
          import os
          import zipfile

          wheels = glob.glob("dist/*.whl")
          assert len(wheels) == 1, wheels
          with zipfile.ZipFile(wheels[0]) as archive:
              metadata_name = next(name for name in archive.namelist() if name.endswith(".dist-info/METADATA"))
              metadata = email.parser.BytesParser().parsebytes(archive.read(metadata_name))
          version = metadata["Version"]
          if os.environ["PUBLISH_TARGET"] == "pypi":
              assert os.environ["RELEASE_REF"] == f"v{version}", (
                  os.environ["RELEASE_REF"], version
              )
          PY
      - name: Publish exact verified distributions
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: ${{ (github.event_name == 'push' || inputs.target == 'testpypi') && 'https://test.pypi.org/legacy/' || 'https://upload.pypi.org/legacy/' }}
